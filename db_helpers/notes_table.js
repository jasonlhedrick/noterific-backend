const db = require('./config_db');

async function createTable() {
    
}

async function dropTable() {
    
}

async function add(note) {
   
}

module.exports = {
    createTable,
    dropTable,
    add,
}